node_modules/
dist/
data/
# Throwaway clone of the live database made by scripts/audit-db.ts. Carries a
# copy of master.key, so it must never be committed even though its access code
# is a placeholder.
data-audit/
# Isolated verify instance used by agents; same reason as data-audit.
data-verify/
# Bundled Node and cloudflared binaries: ~150 MB, installed per machine.
runtime/
# Pid files and process logs written by scripts/start.ps1.
run/
# Renders copied out by scripts/verify-generation-live.ts, for looking at once.
probe-out/
*.tsbuildinfo
*.log
.env*
!.env.example
__pycache__/
*.pyc
